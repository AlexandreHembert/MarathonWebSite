{% extends 'base.html.twig' %}

{% block title %}Hello{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h3>Création d'une Histoire</h3>
        {{ form_start(formCreerHistoire) }}
        {{ form_widget(formCreerHistoire) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
        {{ form_end(formCreerHistoire) }}
    </div>
{% endblock %}